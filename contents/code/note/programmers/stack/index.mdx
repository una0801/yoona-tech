---
title: 컨트롤 제트
description: 스택(stack) 자료구조 활용
tags: [programmers]
---
## 문제
```
숫자와 "Z"가 공백으로 구분되어 담긴 문자열이 주어집니다. 
문자열에 있는 숫자를 차례대로 더하려고 합니다. 
이 때 "Z"가 나오면 바로 전에 더했던 숫자를 뺀다는 뜻입니다. 
숫자와 "Z"로 이루어진 문자열 s가 주어질 때, 머쓱이가 구한 값을 return 하도록 solution 함수를 완성해보세요.

✅ 제한사항
- 1 ≤ s의 길이 ≤ 200
- 1,000 < s의 원소 중 숫자 < 1,000
- s는 숫자, "Z", 공백으로 이루어져 있습니다.
- s에 있는 숫자와 "Z"는 서로 공백으로 구분됩니다.
- 연속된 공백은 주어지지 않습니다.
- 0을 제외하고는 0으로 시작하는 숫자는 없습니다.
- s는 "Z"로 시작하지 않습니다.
- s의 시작과 끝에는 공백이 없습니다.
- "Z"가 연속해서 나오는 경우는 없습니다.

✅ 입출력 예 설명
- 입출력 예 #1
    10 + 20 + 30 + 40 = 100을 return 합니다.
- 입출력 예 #2
    "10 Z 20 Z 1"에서 10 다음 Z, 20 다음 Z로 10, 20이 지워지고 1만 더하여 1을 return 합니다.
```

## 풀이
- 스택 생성: 숫자들을 저장할 빈 스택을 생성
- 문자열 분리: 주어진 문자열을 공백 기준으로 분리하여 각 토큰 얻기
- 순회하며 처리:
    - 토큰이 숫자일 경우: 스택에 숫자를 추가(push)
    - 토큰이 "Z"일 경우: 스택에서 마지막에 추가한 숫자를 제거(pop)
```python
# 내가  푼 답(인덱싱 방식)
def solution(s):
    answer = 0
    find_z = s.split()
    
    for i in range(len(find_z)):
        if find_z[i] == 'Z':
            answer -= int(find_z[i-1])
        else: answer += int(find_z[i])
    return answer
☑️ 인덱싱 방식은 문제의 제약 조건이 맞아떨어질 때는 간단하지만,
  만약 "Z"의 위치나 연속성 등 조건이 바뀐다면 오류가 발생할 수 있음
# Stack 사용 풀이
def solution(s):
    tokens = s.split()
    stack = []
    for token in tokens:
        if token == "Z":
            stack.pop()  # 마지막에 추가한 숫자 제거
        else:
            stack.append(int(token))
    return sum(stack)
```