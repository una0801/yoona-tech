---
title: 선형 검색 - 가까운 수 찾기
description: 선형 검색(탐색)) 알고리즘
tags: [programmers, search]
---
## 문제
```
정수 배열 array와 정수 n이 매개변수로 주어질 때, 
array에 들어있는 정수 중 n과 가장 가까운 수를 return 하도록 solution 함수를 완성해주세요.

✅ 제한사항
- 1 ≤ array의 길이 ≤ 100
- 1 ≤ array의 원소 ≤ 100
- 1 ≤ n ≤ 100
- 가장 가까운 수가 여러 개일 경우 더 작은 수를 return 합니다.

✅ 입출력 예 설명
- 입출력 예 #1
    3, 10, 28 중 20과 가장 가까운 수는 28입니다.
- 입출력 예 #2
    10, 11, 12 중 13과 가장 가까운 수는 12입니다.
```

## 풀이
- 각 숫자 x에 대해, n과의 차이는 abs(x - n)로 구할 수 있음
- 만약 두 숫자의 차이가 같다면, 더 작은 숫자를 선택
- 비교 기준(키)를 정하기
    - 각 원소 x를 비교할 때 두 가지 기준이 필요
        - 첫 번째 기준: abs(x - n)
         → 이 값이 작을수록 n과 가까움
        - 두 번째 기준: x
         → 차이가 같을 경우, 작은 값이 우선
    - 두 기준을 하나의 튜플로 만들면 `(abs(x - n), x)`
    -  튜플 비교 시 첫 번째 원소를 먼저 비교하고, 같으면 두 번째 원소를 비교
```python
def solution(array, n):
    return min(array, key=lambda x: (abs(x - n), x))
```