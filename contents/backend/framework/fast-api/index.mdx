---
title: FastAPI 란?
description: Python 기반 웹 프레임워크
tags: [framework]
---

## 기능
- 비동기 지원
- 경로 매개변수(Path Parameters) 및 쿼리 매개변수(Query Parameters)
- Pydantic을 활용한 요청 및 응답 데이터 검증
- OAuth2 및 JWT 기반 인증

  ☑️ 장점 
    - 빠른 성능
      - Starlette(비동기 Python 웹 프레임워크): 비동기 방식의 동시 작업 진행
      - pydantic : 효율적인 데이터 검증과 타입 변환, 고급 문자열 포맷팅, 변수 어노테이션, 데이터 클래스 등을 효율적으로 사용
    - 경량 & 빠른 개발 속도
      - 최소한의 코드로 API 개발이 가능하여 빠른 개발과 유지보수 용이
      - flask 보다 생산성이 높으며 Django 보다 가벼운 프레임워크
    - 자동 문서 생성 (swagger UI, ReDoc)
    - 타입 힌트 지원
      - 타입 힌트를 활용한 IDE 자동완성 및 코드 가독성이 뛰어남
    - Dependency Injectioin (DI) 지원
    - 다양한 ORM 및 데이터베이스 지원

  ☑️ 단점 
    - 비동기 DB 연동 시 난이도 증가
      - SQLAlchemy의 기본 버전은 동기방식이므로, 비동기 사용 시 추가 설정 필요
      - ORM에서 Async/Await을 올바르게 사용해야 하며, `async_session` 관리가 필요
    - 성숙도가 Django보다 낮음
      - Django처럼 대형 프로젝트를 위한 내장 기능(ORM, Admin, Auth 등)이 부족하여 직접 구축해야 함
      - Django Rest Framework(DRF) 대비 문서와 커뮤니티가 상대적으로 작음
    - 동기 라이브러리와 함꼐 사용 시 비효율 발생
      -`async` 기반으로 설계되었기 때문에 동기(Sync) 라이브러리와 혼용하면 `ThreadPoolExecutor` 등을 사용해야 하는 경우가 발생
      - 잘못된 비동기 코드 작성 시 성능 저하 및 병목 현상이 생길 수 있음
    - WebSocket 및 Background Task 제약
      - WebSocket, SSE(Server-Sent Events) 등의 실시간 통신 구현은 가능하지만, Django Channels보다 기능이 제한적
      - Celery와 같은 전문적인 Task Queue를 사용하지 않으면 장기 실행 작업(Background Task) 관리가 어려울 수 있음

## 자동 API 문서화
- OpenAPI(Swagger UI) 및 ReDoc을 자동 생성하여 API 문서를 제공
- `http://localhost:8000/docs` → Swagger UI
- `http://localhost:8000/redoc` → ReDoc
<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
  </TabsList>
  <TabsContent value="python">
    ```python
    from fastapi import FastAPI
    app = FastAPI()

    @app.get("/items/{item_id}")
    async def read_item(item_id: int, q: str = None):
        return {"item_id": item_id, "q": q}
    ```
  </TabsContent>
</Tabs>

## 비동기 지원
- 비동기 I/O 기반으로 `asnyc def` 사용 가능
- 대량의 요청을 효율적으로 처리할 수 있어 API 성능 최적화에 유리
<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
  </TabsList>
  <TabsContent value="python">
    ```python
    from fastapi import FastAPI
    import asyncio

    app = FastAPI()

    @app.get("/async-task")
    async def async_task():
        await asyncio.sleep(2)  # 비동기 작업
        return {"message": "완료됨"}
    ```
  </TabsContent>
</Tabs>

## 경로 매개변수 및 쿼리 매개변수
- URL 경로를 동적으로 설정 가능
- 쿼리 문자열을 처리하는 기능 지원
<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
  </TabsList>
  <TabsContent value="python">
    ```python
    @app.get("/users/{user_id}")
    async def get_user(user_id: int, active: bool = True):
        return {"user_id": user_id, "active": active}
    ```
  </TabsContent>
</Tabs>

## pydantic을 활용
- 요청 및 응답 데이터를 강력하게 검증
- 데이터 타입을 자동 변환 및 검증
<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
  </TabsList>
  <TabsContent value="python">
    ```python
    from pydantic import BaseModel

    class Item(BaseModel):
        name: str
        price: float
        is_offer: bool = False

    @app.post("/items/")
    async def create_item(item: Item):
        return {"item_name": item.name, "item_price": item.price}
    ```
  </TabsContent>
</Tabs>

## Dependency Injection
- `Depends` 기능을 활용한 재사용
<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
  </TabsList>
  <TabsContent value="python">
    ```python
    from fastapi import Depends

    async def common_parameters(q: str = None, limit: int = 10):
        return {"q": q, "limit": limit}

    @app.get("/items/")
    async def read_items(params: dict = Depends(common_parameters)):
        return params
    ```
  </TabsContent>
</Tabs>

## OAuth2 및 JWT 기반 인증
<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
  </TabsList>
  <TabsContent value="python">
    ```python
    from fastapi.security import OAuth2PasswordBearer

    oauth2_scheme = OAuth2PasswordBearer(tokenUrl="token")

    @app.get("/users/me")
    async def read_users_me(token: str = Depends(oauth2_scheme)):
        return {"token": token}
    ```
  </TabsContent>
</Tabs>

## Middleware 및 CORS
<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
  </TabsList>
  <TabsContent value="python">
    ```python
    from fastapi.middleware.cors import CORSMiddleware

    app.add_middleware(
        CORSMiddleware,
        allow_origins=["*"],
        allow_credentials=True,
        allow_methods=["*"],
        allow_headers=["*"],
    )
    ```
  </TabsContent>
</Tabs>

## Background Tasks
<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
  </TabsList>
  <TabsContent value="python">
    ```python
    from fastapi import BackgroundTasks

    def write_log(message: str):
        with open("log.txt", "a") as log:
            log.write(message + "\n")

    @app.post("/log/")
    async def log_message(message: str, background_tasks: BackgroundTasks):
        background_tasks.add_task(write_log, message)
        return {"message": "로그 저장 중"}
    ```
  </TabsContent>
</Tabs>

## Websockets
<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
  </TabsList>
  <TabsContent value="python">
    ```python
    from fastapi import WebSocket

    @app.websocket("/ws")
    async def websocket_endpoint(websocket: WebSocket):
        await websocket.accept()
        while True:
            data = await websocket.receive_text()
            await websocket.send_text(f"메시지 수신: {data}")
    ```
  </TabsContent>
</Tabs>

## 스트리밍 응답
- `StreamingResponse`를 사용하여 대용량 데이터 스트리밍
<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
  </TabsList>
  <TabsContent value="python">
    ```python
    from fastapi.responses import StreamingResponse

    async def fake_video_streamer():
        for i in range(10):
            yield f"frame {i}\n"

    @app.get("/video/")
    async def video_stream():
        return StreamingResponse(fake_video_streamer(), media_type="text/plain")
    ```
  </TabsContent>
</Tabs>

## 파일 업로드 및 다운로드
- `UploadFile`을 사용하여 파일 업로드 가능
<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
  </TabsList>
  <TabsContent value="python">
    ```python
    from fastapi.responses import StreamingResponse

    async def fake_video_streamer():
        for i in range(10):
            yield f"frame {i}\n"

    @app.get("/video/")
    async def video_stream():
        return StreamingResponse(fake_video_streamer(), media_type="text/plain")
    ```
  </TabsContent>
</Tabs>

## GraphQL 지원
<Tabs defaultValue="python" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="python">Python</TabsTrigger>
  </TabsList>
  <TabsContent value="python">
    ```python
    from fastapi import FastAPI
    from fastapi.middleware.cors import CORSMiddleware
    from strawberry.fastapi import GraphQLRouter
    import strawberry

    @strawberry.type
    class Query:
        hello: str = "Hello GraphQL!"

    schema = strawberry.Schema(query=Query)
    graphql_app = GraphQLRouter(schema)

    app = FastAPI()
    app.include_router(graphql_app, prefix="/graphql")
    ```
  </TabsContent>
</Tabs>